import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  experimental: {
    // Server Actions are enabled by default in Next 15; add options here if needed.
  },
};

export default nextConfig;
